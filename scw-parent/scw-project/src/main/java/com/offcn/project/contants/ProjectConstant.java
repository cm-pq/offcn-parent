package com.offcn.project.contants;

public class ProjectConstant {
    //拼接在projectToken前。用于redis中的key
    //其中属性值:号代表的是redis文件夹.....路径
    public static final String TEMP_PROJECT_PREFIX = "project:create:temp:";
}